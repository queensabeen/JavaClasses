package com.syntax.class20;

public class Task4 {
    //Write program that has static constructors and observe the result

    //static Task4(){
    // constructor cannot be static – Because if we make them static they can't be called from other
    // class thus object of other class cant be created.
    // If you made constructor as static then the constructor
    // will be called before object creation like the main method.

}
